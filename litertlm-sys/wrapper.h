// As of LiteRT-LM's official C API prebuilt package (v0.16.0+), the
// Conversation-related API lives in conversation.h, which itself
// #includes engine.h -- so including just this one file pulls in the
// entire public C API.
#include "conversation.h"
